# wallust v3.4
#
# You can copy this file to ~/.config/wallust/wallust.toml (keep in mind is a sample config)

# SIMPLE TUTORIAL, or `man wallust.5`:
# https://explosion-mental.codeberg.page/wallust/
#
# If comming from v2: https://explosion-mental.codeberg.page/wallust/v3.html#wallusttoml

# Global section - values below can be overwritten by command line flags

# How the image is parse, in order to get the colors:
# full - resized - wal - thumb -  fastresize - kmeans
backend = "fastresize"

# What color space to use to produce and select the most prominent colors:
# lab - labmixed - lch - lchmixed
color_space = "lab"

# Use the most prominent colors in a way that makes sense, a scheme color palette:
# dark - dark16 - darkcomp - darkcomp16
# light - light16 - lightcomp - lightcomp16
# harddark - harddark16 - harddarkcomp - harddarkcomp16
# softdark - softdark16 - softdarkcomp - softdarkcomp16
# softlight - softlight16 - softlightcomp - softlightcomp16
palette = "dark"

# Ensures a "readable contrast" (OPTIONAL, disabled by default)
# Should only be enabled when you notice an unreadable contrast frequently happening
# with your images. The reference color for the contrast is the background color.
#check_contrast = true

# Color saturation, between [1% and 100%] (OPTIONAL, disabled by default)
# usually something higher than 50 increases the saturation and below
# decreases it (on a scheme with strong and vivid colors)
#saturation = 35

# Alpha value for templating, by default 100 (no other use whatsoever)
#alpha = 100

pywal=true

[templates]
# NOTE: prefer '' over "" for paths, avoids escaping.
# template: A RELATIVE path that points to `~/.config/wallust/template` (depends on platform)
# target: ABSOLUTE path in which to place a file with generated templated values.
# ¡ If either one is a directory, then both SHOULD be one. !
# zathura = { template = 'zathura', target = '~/.config/zathura/zathurarc' }
waybar = { template = 'style.css', target = '~/.config/waybar/style.css'}
btop = { template = 'btop.theme', target = '/home/hamato/.config/btop/themes/matugen.theme'}
cava = { template = 'cava-colors.ini', target = '/home/hamato/.config/cava/themes/cava-colors.ini'}
kitty = { template = 'colors-kitty.conf', target = '/home/hamato/.config/kitty/kitty-colors.conf'}
mako = { template = 'colors-mako', target = '/home/hamato/.config/mako/config'}
darkreader = { template = 'darkreader.json', target = '/home/hamato/.cache/wal/darkreader.json'}
gtk3 = { template = 'gtk-colors.css', target = '/home/hamato/.config/gtk-3.0/gtk.css'}
gtk4 = { template = 'gtk-colors.css', target = '/home/hamato/.config/gtk-4.0/gtk.css'}
pywalfox = { template = 'pywalfox-colors.json', target = '/home/hamato/.cache/wal/colors.json'}
qt5ct = { template = 'qtct-colors.conf', target = '/home/hamato/.config/qt5ct/colors/pywal.conf'}
qt6ct = { template = 'qtct-colors.conf', target = '/home/hamato/.config/qt6ct/colors/pywal.conf'}
rofi = { template = 'rofi-config.rasi', target = '/home/hamato/.config/rofi/config.rasi'}
yazi = { template = 'yazi-theme.toml', target = '/home/hamato/.config/yazi/theme.toml'}
zathura = { template = 'zathura-colors', target = '/home/hamato/.config/zathura/zathurarc'}
nvim = { template = 'colors.vim', target = '/home/hamato/.cache/wal/colors-wal.vim'}
pywalkvconfig = { template = 'pywal.kvconfig', target = '/home/hamato/.config/Kvantum/pywal/pywal.kvconfig'}
pywalsvg = { template = 'pywal.svg', target = '/home/hamato/.config/Kvantum/pywal/pywal.svg'}
kdeglobals = { template = 'kdeglobals', target = '/home/hamato/.config/kdeglobals'}
waybarconfig = {template = 'config.jsonc', target = '/home/hamato/.config/waybar/config.jsonc'}
vpn = { template = 'vpn-status.sh', target = '/home/hamato/scripts/vpn-status.sh'}
hyprland = { template = 'colors-hyprland.conf', target = '/home/hamato/.config/hypr/hyprland-colors.conf'}
